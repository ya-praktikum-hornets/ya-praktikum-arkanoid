{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "prestart": "npm run build --prefix client",
    "serve": "cross-env NODE_ENV=development nodemon --config ./nodemon.config.json",
    "build": "cross-env NODE_ENV=production tsc --build tsconfig.json",
    "start": "cross-env NODE_ENV=production node ./build/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.19",
    "nodemon": "^2.0.15"
  }
}
