import styled from 'styled-components';

const ButtonElement = styled.button`
    width: 100%;
    height: 52px;

    border: none;

    font-family: inherit;
    font-weight: 600;
    font-size: 14px;
    color: #fff;
    background: #0B82FF;
    border-radius: 6px;
`;

export { ButtonElement };
